$('#flash').append("<%= escape_javascript (render partial: 'layouts/notice') %>");
$('.cart .content').html("<%= escape_javascript (render partial: 'items/item', collection: client_cart.items.order('created_at desc'), as: :item) %>");
$('.item.subtotal').remove();
$('.cart .content').append("<%= escape_javascript (render partial: 'carts/subtotal') %>");

$('.cart .item form').on('change', function(){
  $(this).submit();
});