$('#buy-domain-stripe').html("<%= escape_javascript (render partial: 'form') %>");

var button;
(function myLoop() {          
   setTimeout(function () { 
    button = $('.content-wrapper #buy-domain-stripe .stripe-button-el');
    if(button){
      button.click();
    } else{
      myLoop();
    } 
   }, 500)
})(10);